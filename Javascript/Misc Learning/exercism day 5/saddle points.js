const originalList = [100, 2, 33, 100, 99];
const indexesOf100 = [];

item, index, array

// int -> [int] -> int
function sortEm( minColOrMaxRow, list4This) {
  if (minColOrMaxRow === 0) {
    return this4Min(list4This)
  }
  if (minColOrMaxRow === 1) {
    return this4Max(list4This)
  }
}

// is it worth doing this and then doing this with a push for the new lists?

// [int] -> int
function this4Min (list4This) {
  return Math.min.apply(list4This)
}

// [int] -> int
function this4Min (list4This) {
  return Math.max.apply(list4This)
}


function saddlePoints(list4This) {


originalList.forEach(
  (num, index) => {
  let maxNum = sortEm()


  Math.max.apply(originalList)
  if (num === maxNum) {
    indexesOf100.push(index);
  }
});

}
