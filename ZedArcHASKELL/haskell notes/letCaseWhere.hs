so its neat that you can use let in so many ways


i forgot this was in hs

dont have tab tho i gotta enable that

shoulda made it a text file, rip



anyways



in ghci you can use let to define an expression

you can also use let to test an expression by adding the in modifer and that makes thte expression unique to that scope only, it wont save the expression


let formula x y = x + y

let cantCallThis x y = x + y in cantCallThis 89 34
